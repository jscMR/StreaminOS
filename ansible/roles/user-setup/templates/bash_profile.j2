# StreaminOS Bash Profile
# Auto-generated by Ansible - DO NOT EDIT MANUALLY

# Source environment variables
if [ -f "$HOME/.profile" ]; then
    . "$HOME/.profile"
fi

# Source bashrc if running bash
if [ -n "$BASH_VERSION" ]; then
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
fi
