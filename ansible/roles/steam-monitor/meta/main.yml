---
# Meta information for steam-monitor role

galaxy_info:
    author: StreaminOS
    description: Automatically detect and register Steam games in Sunshine for streaming
    license: MIT
    min_ansible_version: "2.9"

    platforms:
        - name: ArchLinux
          versions:
              - all

    galaxy_tags:
        - gaming
        - steam
        - streaming
        - sunshine
        - moonlight
        - automation

dependencies: []
    # No dependencies needed - install order managed by playbook
    # steam-monitor requires: steam, sunshine (installed before in playbook)
